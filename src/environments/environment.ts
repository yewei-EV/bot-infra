export const AppConfig = {
  production: false,
  environment: 'LOCAL',
  baseUrl: '/api',
  baseUrl2: '/ticket',
  isActivated: true,
  headless: false,
  longerTimeout: 2400000,
  defaultTimeout: 30000,
  devtools: true
};

